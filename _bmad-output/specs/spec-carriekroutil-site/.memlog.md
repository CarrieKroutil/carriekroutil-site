---
topic: carriekroutil.com — personal Hugo+Hextra static site: spec adoption of finalized PRD + architecture spine
updated: 2026-06-22T18:44
---

- (note) ping-test
- (direction) Carrie: lead with bmad-spec — adopt the architecture spine as a spec companion so AD IDs stay stable and downstream (epics/stories) can cite them
- (decision) Spec-adoption run, not greenfield distill: inputs are a FINAL PRD (18 FRs, 5 NFRs) + a FINAL architecture spine (AD-1..AD-13). Trust authored separation; lift, don't re-sort
- (decision) Capability IDs mirror PRD FR numbers 1:1 (CAP-N == FR-N) so FR->CAP->AD cross-citation is unambiguous and nothing gets renumbered downstream
- (decision) ARCHITECTURE-SPINE.md, prd.md, UX DESIGN.md, UX EXPERIENCE.md are ADOPTED companions (referenced, not edited) — downstream reads FR detail, AD rules, visual tokens, journeys
- (decision) NFR-1..NFR-5 plus inviolable ADs (AD-1 authoring loop, AD-2 one pipeline, AD-13 no-JS core) become kernel Constraints; full AD set stays in the spine, not restated
- (decision) PRD Open Question #5 (Hextra blog-capability spike) is RESOLVED — the architecture spine commits to the blog surfaces; not carried forward as open
- (note) The earlier 'ping-test' note entry is a stray diagnostic, not a real decision; SPEC.md is derived and ignores it
- (capability) CAP-1 (FR-1, AD-6/AD-13): Home hero introduces Carrie — / renders wordmark, avatar, greeting, one-liner, clickable topic chips. Success: / shows all five and each chip links /tags/{topic}/; readable with JS off
- (capability) CAP-2 (FR-2, AD-6): Home previews latest posts — 4-6 most-recent posts as full-card link targets, newest first, with an 'all posts ->' affordance. Success: cards show title/tag/excerpt/date+read-time and whole card links the post page
- (capability) CAP-3 (FR-3, AD-13): About page presents Carrie — /about/ shows photo, bio, topic chips, GitHub + LinkedIn links. Success: links open github.com/carriekroutil and linkedin.com/in/carriekroutil
- (capability) CAP-4 (FR-4, AD-4/AD-8): Browse all posts — /posts/ lists every non-draft post reverse-chron; narrowable to one topic via tag term-pages (no client JS). Success: /posts/ full stream + /tags/{topic}/ filtered list, each entry links its post
- (capability) CAP-5 (FR-5, AD-4/AD-9): Read a single post — /posts/{slug}/ renders title, date, read-time, clickable tags, markdown body with syntax-highlighted code and alt-texted images; drafts excluded. Success: post page renders all of these + a way back to the stream
- (capability) CAP-6 (FR-6, AD-3): Read-time and metadata — each post shows auto-derived read-time (.ReadingTime, 'N min read', floor 1) and front-matter date. Success: read-time never hand-entered; missing/invalid date fails the build, never mis-sorts silently
- (capability) CAP-7 (FR-7, AD-3/AD-8): Tag a post by topic — author assigns topics via lowercase 'tags' front-matter; open taxonomy. Success: tags render as chips on post + cards; a new tag value needs no code change
- (capability) CAP-8 (FR-8, AD-8): Browse a topic — clicking a chip opens /tags/{topic}/ listing all posts under it, newest first. Success: term page lists the topic's posts; empty term page shows a light 'check back' state, not blank
- (capability) CAP-9 (FR-9, AD-13): Search posts — header control opens Hextra FlexSearch, filtering title/content as you type, keyboard-navigable. Success: results filter live; search is the one accepted JS-only feature and degrades gracefully with JS off
- (capability) CAP-10 (FR-10, AD-1/AD-2/AD-11): Publish by push — adding a markdown file and pushing the default branch triggers an Amplify build that deploys; no CMS/login/copy step. Success: new non-draft post is live at its page + in the stream within a few minutes
- (capability) CAP-11 (FR-11, AD-2): Single settled pipeline on the correct brand — exactly one Amplify pipeline builds carriekroutil-site, served at carriekroutil.com via Route 53. Success: baseURL/title are carriekroutil.com; zero SheBytes/code.shebytes.io/ananke/quickstart remnants; no second deploy path in-repo
- (capability) CAP-12 (FR-12, AD conventions/UX spine): Brand identity consistent and on-palette — gradient wordmark links Home with #d946ef solid fallback; accents stay a garnish per the UX spine. Success: wordmark renders gradient (fallback under forced-colors/print); no large gradient fill behind body text
- (capability) CAP-13 (FR-13, AD conventions): Dark/light theme toggle — dark default, a real focusable labeled toggle to light, choice persists (Hextra). Success: loads dark; toggle switches+persists across visits
- (capability) CAP-14 (FR-14, Deferred): Lightweight privacy-friendly readership signal — once chosen, basic page views via one privacy-friendly script include (GoatCounter or Cloudflare Web Analytics, not Mixpanel). Success: single snippet, no cookie banner, no PII. NOTE: tool choice deferred, not a launch gate
- (capability) CAP-15 (FR-15, AD-7/AD-6): Featured post on Home — one post flagged featured:true holds a single Home slot independent of recency. Success: featured post stays visible as newer posts publish; falls back cleanly to most-recent when none flagged
- (capability) CAP-16 (FR-16, AD-2): Preview locally before publishing — 'hugo server' renders the site incl. the new/edited post with live reload, showing draft + non-draft. Success: Carrie sees the post in a browser locally before pushing
- (capability) CAP-17 (FR-17, AD-12): Build outcomes are surfaced — a failed Amplify build emails Carrie and keeps the prior good site live, never a silent no-op. Success: build failure notifies; a successful build with no new post is distinguishable from a failure
- (capability) CAP-18 (FR-18, AD conventions): Basic discoverability — posts crawlable and unfurl cleanly when shared. Success: site emits sitemap + indexing robots.txt; each post page has descriptive title/meta-description/OG+Twitter tags reading hero.src/hero.alt; URLs are stable human-readable /posts/{slug}/
- (constraint) Authoring loop is inviolable (NFR-4/AD-1): publishing is exactly write-markdown -> git push. No CMS, login, web editor, or manual copy/upload may ever enter the path; any feature adding an authoring step is a regression
- (constraint) Exactly one deploy pipeline (AD-2/AD-11): AWS Amplify only, building from source on the default branch, served at carriekroutil.com via Route 53. Branch/PR auto-previews off; one environment (production); no S3-sync action or GCP artifacts in-repo
- (constraint) Core reading works without JavaScript (NFR-1/AD-13): Home, post, stream, about, tag pages render as static HTML with JS off. Hextra FlexSearch is the single accepted JS-dependent exception. No web-font network load (system fonts only)
- (constraint) Static, no server backend (NFR-5): static files only; git repo is the single source of truth; Carrie owns domain, content (plain markdown), and pipeline — portable off Amplify, no platform lock-in
- (constraint) Accessibility floor (NFR-2): dark (default) theme meets WCAG AA per UX review; structural a11y holds in both themes (one h1, heading order, landmarks, 2px #d946ef focus ring, real a/button with labels, alt on every image enforced build-failing, prefers-reduced-motion). Light mode ships untuned, not yet AA-verified
- (constraint) Privacy (NFR-3): no consent-requiring cookies, no PII; any analytics is a single privacy-friendly script include only
- (constraint) Visual tokens owned by the UX spine (DESIGN.md) — palette, 920px content / 720px reading width, gradient-as-garnish — authoritative; spec/architecture do not restate them
- (constraint) Post front-matter contract is canonical (AD-3): YAML; title, date (ISO, invalid fails build), tags (lowercase, open), draft, optional featured, optional hero {src, alt-required}; read-time derived
- (non-goal) No interactivity / UGC — no comments, accounts, reactions, or visitor-submitted content; read-only, single-author
- (non-goal) No CMS or web editor — markdown-and-push IS the product; an admin UI defeats the purpose
- (non-goal) No newsletter / email capture and no heavy product analytics in v1
- (non-goal) No resume/CV section — professional signal is woven through content, not a credential list
- (non-goal) No docs/guides section in v1 (Hextra could add later)
- (non-goal) No server backend; no carry-over of SheBytes branding or throwaway quickstart content
- (non-goal) Don't chase traffic (SM-C1): raw pageviews/visitors are not a success target and must not drive decisions
- (non-goal) Don't add friction (SM-C2): number of features is not a goal; any addition that complicates the authoring loop is a regression
- (decision) Success signal = SM-1 (the habit holds: still publishing ~1-2 posts/month a year, ideally three, from now) + SM-2 (publish path stays exactly write->git push, zero added manual steps), with SM-3 (the BMAD launch post lands and is sendable to both a friend and a recruiter) as the concrete launch proof
- (question) Analytics tool (CAP-14/FR-14): GoatCounter vs Cloudflare Web Analytics — deferred, not a launch gate; pick at/just after launch (owner Carrie)
- (question) Light-mode AA (NFR-2): re-verify the three accent colors meet WCAG AA on Hextra light surfaces before relying on light mode
- (question) Custom 404 ('wandered off the fairway'): confirm acceptable to defer past v1 (non-blocking)
- (question) Legacy deploy cleanup: manual one-time task in old ~/code/quickstart (disable S3-sync action, remove GCP app.yaml/main.go/.gcloudignore) so the retired project stops auto-deploying — outside this repo; FR-11 only requires not carrying them in (owner Carrie, manual)
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6,8): PASS. All 18 caps have intent+success; intents are WHAT (mechanism names like FlexSearch/hugo server/Amplify sit in success or come from adopted ADs); 8 constraints each rule something out; 8 non-goals; success signal is demonstrable (push->live in minutes; still publishing in a year; launch post sendable); CAP IDs stable/unique and mirror FR-N
- (event) Self-validate Pass 2 (preservation): PASS. 18 FRs -> CAP-1..18; 5 NFRs -> Constraints; non-goals §5 + counter-metrics SM-C1/SM-C2 -> Non-goals; SM-1/2/3 -> Success signal (SM-4 covered via CAP-12 + perf constraint); §8 open questions -> 4 carried, OQ#5 resolved. Glossary §3, AD-1..13, and Stack are NOT restated — held in adopted PRD + spine companions by design. PRD §10 assumptions distributed into relevant CAP success lines/constraints/open-questions; PRD retains the canonical index
- (event) Wrapper-only content dropped (logged, not silent): PRD §0 Document Purpose and §10 Assumptions Index header (process/traceability framing). No load-bearing claim dropped
- (event) Spec finalized — SPEC-carriekroutil-site created with 18 capabilities and 4 adopted companions (ARCHITECTURE-SPINE, prd, DESIGN, EXPERIENCE). Ready for bmad-create-epics-and-stories
