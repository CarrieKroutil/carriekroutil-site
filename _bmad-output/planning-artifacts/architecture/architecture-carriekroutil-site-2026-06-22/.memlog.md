---
scope: carriekroutil.com — personal Hugo static site (landing/about + tagged post stream), authored markdown→push→Amplify deploy
purpose: build-substrate spine for downstream epics/stories; resolve 5 architecture-deferred decisions
altitude: feature
updated: 2026-06-22T15:28
---

- (constraint) [ADOPTED from brief/PRD] Stack: Hugo static-site generator + Hextra theme installed via Hugo Modules; no server backend, static files only. Fallback theme if Hextra disappoints: FixIt.
- (constraint) [ADOPTED from brief/PRD] Deploy: single AWS Amplify pipeline building from source on push to default branch of carriekroutil-site repo; domain carriekroutil.com on Route 53. Exactly one pipeline; no S3-sync action, no GCP artifacts carried in.
- (constraint) [ADOPTED from brief/PRD] Authoring loop is the north-star invariant: write markdown file -> git push -> live in minutes. No CMS, no login, no manual copy step. Any added manual step is a regression (SM-2).
- (constraint) [ADOPTED from PRD] Content model: each Post is a markdown file, optionally a Hugo page bundle pairing markdown with images; front matter carries title, date, tags, draft, optional hero image with alt text.
- (constraint) [ADOPTED from PRD] Open tag taxonomy (golf/dogs/code/AI/leadership/trips at launch) with no fixed allowlist; adding a new tag value requires no code change.
- (constraint) [ADOPTED from UX DESIGN] Dark theme is default and is the AA-guaranteed v1 path; light mode ships untuned Hextra defaults (AA deferred). Visual tokens (palette, 920px content / 720px reading width, system-font stacks, gradient-as-garnish) are owned by the UX spine, authoritative.
- (constraint) [ADOPTED from PRD] Core reading works without JavaScript; search (Hextra FlexSearch) is the accepted JS-dependent exception. No web-font network load; images lazy-load.
- (constraint) [ADOPTED from PRD] URLs: posts at /posts/{slug}/ stable human-readable slugs; tag pages at /tags/{topic}/; about at /about/; full stream at /posts/. Drafts excluded from stream and build output.
- (event) Activation: Coaching path chosen. Deliverables = spine + build-in-public capture notes + readable walkthrough doc. Hextra blog-capability spike dispatched.
- (version) Hextra spike (web-verified 2026-06): Hextra v0.12.3 (released 2026-05-05, actively maintained), installs via Hugo Modules (path github.com/imfing/hextra), needs Hugo extended (current 0.14x+). NATIVE: blog section list with preview cards (title/date/excerpt/tag-chips)+pagination, /tags/ + /tags/{tag}/ term pages with chips, FlexSearch full-text search (on by default, indexes posts), dark-default + toggle (params.theme.default=dark), lazy image loading. NOT native (custom work): read-time surfacing, featured/pinned post, tag chips on single-post page, home latest-posts feed, responsive/WebP image processing. Hextra blog layout activates on a section whose _index.md sets type: blog.
- (decision) [ADOPTED] Front-matter format = YAML (---) for all posts (Hextra community default; clean snippet reuse). Binds: every post file. Prevents: posts diverging between TOML/YAML, snippet-translation friction.
- (decision) AD: Home preview cards + featured-post card are a CUSTOM Hugo partial (e.g. layouts/_partials/custom/blog-cards.html) that queries recent/featured posts and renders the card design ported from the finalized UX home.html mockup, injected into Hextra's home layout. Binds: FR-2, FR-15, Home. Prevents: assuming Hextra auto-populates a latest-posts feed (it does not); divergent card markup between home and list. Native hextra-card does NOT match the UX card design.
- (decision) AD: ALL theme customizations (custom partials, layout overrides, read-time, tag chips on single, CSS) live in the project's own layouts/ and assets/ shadowing the Hextra module; the Hextra module is NEVER edited in place. Binds: all custom layout/style work, NFR-4 longevity. Prevents: edits lost on theme upgrade, divergence between two override files placed in different locations.
- (decision) AD: Featured post marked by front-matter 'featured: true' (YAML boolean) on the post itself. Home cards partial queries where featured==true, renders exactly one featured slot, falls back cleanly to most-recent when none flagged. Binds: FR-15, Home, authoring loop. Prevents: a second non-markdown place to curate (keeps authoring markdown-only), empty featured slot.
- (decision) AD: Images via Hugo page bundles (post = folder, images beside markdown, self-contained/portable). Hero image: front-matter field carrying a REQUIRED alt string. Inline images: standard markdown with required alt. Hextra lazy-loads natively. Resize/responsive/WebP DEFERRED (no render hook in v1; revisit if images get heavy). Binds: FR-5, NFR-1, NFR-2. Prevents: images detached from posts, missing alt text, build complexity at launch.
- (decision) AD: Topic filtering = tag term pages only. A Topic Chip links to /tags/{topic}/, rendered natively by Hextra as the filtered list; /posts/ stays the full reverse-chron stream. NO client-side JS filtering. Binds: FR-4, FR-8, NFR (no-JS core). Prevents: a JS dependency on a core reading surface, custom filter code to maintain.
- (decision) [ADOPTED] Convention: post stream is the section content/posts/ with _index.md setting type: blog -> keeps PRD /posts/{slug}/ URLs AND activates Hextra's native blog list+single layouts. Binds: FR-4, FR-5, URL scheme. Prevents: choosing content/blog/ (Hextra default) which would change the public URL, or a plain section that misses Hextra's blog layout.
- (decision) AD (front-matter contract): every Post carries YAML front matter with: title (string), date (RFC-3339/ISO date; invalid/missing surfaces at build), tags (list; open taxonomy), draft (bool; drafts excluded from build+stream), optional featured (bool), optional hero image field with required alt. Read-time is DERIVED from .ReadingTime (never hand-entered). Binds: all posts, FR-5/6/7/15. Prevents: independently-authored posts diverging on field names/shapes; the single highest-divergence-risk surface.
- (decision) AD: Version pinning for reproducibility (NFR-4). Hugo version pinned via Amplify build env (HUGO_VERSION, Hugo extended); Hextra module version locked in go.mod. Upgrades are deliberate (hugo mod get -u + re-test), never automatic. Binds: deploy pipeline, NFR-4. Prevents: a future Hugo/Hextra release silently breaking a content-only push.
- (decision) AD: Build-failure notification = Amplify native email-on-failure (FR-17). Enabled once in Amplify console, no extra infra/code. Amplify swaps to new build only on success, so a failed build leaves the prior good site live AND emails Carrie. Binds: FR-17, authoring loop. Prevents: a silent broken publish.
- (decision) AD (deploy build spec): amplify.yml at repo root is the single source of build truth; preBuild fetches the Hextra module (hugo mod get), build runs Hugo extended (e.g. hugo --gc --minify) at pinned HUGO_VERSION, artifacts = public/. Binds: FR-10, FR-11, single-pipeline rule. Prevents: build config drift, a second deploy path, unpinned Hugo in CI.
- (decision) [ADOPTED] Convention (branding): hugo.toml baseURL=https://carriekroutil.com/ and title reflect carriekroutil.com; zero SheBytes/code.shebytes.io remnants; no ananke theme, no quickstart dummy posts, no S3-sync action or GCP artifacts carried into the repo. Binds: FR-11. Prevents: retired-project branding/deploy paths leaking into v1.
- (decision) Paradigm: build-time static generation, content-as-source. A pure pipeline (markdown+front-matter files in git -> Hugo+Hextra build -> static HTML in public/ -> Amplify CDN). No runtime/server state; the git repo is the single source of truth; every build is reproducible from source. Binds: whole system. Prevents: introducing runtime state, a backend, or a second source of content.
- (question) Deferred (not launch gates): analytics tool choice GoatCounter vs Cloudflare Web Analytics (FR-14, single privacy-friendly script include, pick at/just after launch); light-mode AA re-verification (NFR-2); custom 404 'wandered off the fairway'; image resize/WebP render hook; legacy deploy cleanup in ~/code/quickstart is Carrie's manual one-time task, outside this repo.
- (event) Reviewer gate ran: lint (4 low = false-positive route tokens /posts/{slug}, /tags/{topic}); rubric=PASS-WITH-FIXES; adversarial=HOLES-FOUND (content contract); tech-currency=ISSUES-FOUND (AD-8 term pages, Hugo version).
- (decision) AD-8 CORRECTION (tech-currency): Hextra does NOT ship a styled taxonomy/term layout (per maintainer discussion #330, contradicting the earlier spike's claim). Hugo CORE generates /tags/{topic}/ term pages and the tag-filtering is native Hugo; styling them as a post list is a CUSTOM layouts/_default/term.html override (per AD-5). Chips still link to /tags/{topic}/. Tightens AD-8 + adds term.html to structural seed.
- (decision) AD-3 tightening (adversarial): pin hero field shape = front-matter 'hero' map { src (path in bundle), alt (REQUIRED string) }; the FR-18 OG/Twitter partial reads hero.src/hero.alt (single source). Pin read-time render = '{n} min read', floor 1 min. Prevents: two posts naming the hero field differently; divergent read-time strings.
- (decision) AD-4 tightening (adversarial): slug authority = the post bundle FOLDER NAME derives the slug; front-matter 'slug:' permitted ONLY as a deliberate override; the title never determines the URL. Section-level config lives only in content/posts/_index.md. Prevents: two posts colliding/diverging on URL source.
- (decision) AD-7 tightening (adversarial): if multiple posts set featured:true, the home partial sorts featured candidates by date DESC and takes the first (deterministic tiebreak); the most-recent fallback uses the same date-desc ordering. Prevents: nondeterministic featured pick from Hugo default order.
- (decision) AD-6 tightening (adversarial): the custom card partial is the SINGLE renderer for BOTH the Home preview grid AND the /posts/ list (list template overridden to call the same partial). Prevents: home card and list card drifting into two designs.
- (decision) AD-9 tightening (adversarial/rubric): 'required alt' is made REAL via a minimal layouts/_default/_markup/render-image.html markup hook that FAILS the build on empty alt (markup-only; distinct from the still-deferred resize/WebP processing). Prevents: posts silently shipping images with no alt (NFR-2).
- (decision) Conventions added (adversarial): display date format pinned site-wide (Hugo layout 'Jan 2, 2006'); tags authored lowercase to avoid case-split term pages under open taxonomy; buildFuture=false so future-dated posts are intentionally withheld until their date (draft:true is the explicit withhold mechanism, not future-dating).
- (decision) AD-2/environments tightening (rubric): Amplify builds the DEFAULT BRANCH ONLY; branch/PR auto-previews are OFF (single-pipeline rule); local 'hugo server' (FR-16) is the only pre-publish preview surface. Closes the silent environments dimension.
- (version) Stack correction (tech-currency): Hugo (extended) floor = 0.146.0 (Hextra v0.12.3's min_version); current Hugo is ~0.163.x (2026-06). Pin a current release via HUGO_VERSION; re-check extended/standard edition at upgrade time. Hextra v0.12.3 confirmed current. FixIt softened to 'candidate fallback' (not independently vetted).
- (event) Reviewer-gate fixes applied (AD-2/3/4/6/7/8/9 tightened, stack version corrected, term.html + render-image hook added to seed). Spine finalized: status=final.
- (event) Deliverables produced: ARCHITECTURE-SPINE.md (build substrate, final), BUILD-IN-PUBLIC-NOTES.md (launch-post fodder), walkthrough.html (on-brand readable solution design). Session complete.
