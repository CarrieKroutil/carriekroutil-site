---
topic: carriekroutil.com personal landing page + multi-topic posts
updated: 2026-06-22T13:20
---

- (decision) Intent=Create. Product: carriekroutil.com — personal landing page about Carrie + an easy-to-grow stream of posts across mixed topics (golf, dogs, code, AI, leadership, trips). Repo/operating folder = carriekroutil-site.
- (event) Brownfield: site already exists at ~/code/quickstart (Hugo + ananke theme, 3 dummy posts). Add-post = hugo new + hugo build + push. Technical state captured to addendum.md.
- (event) Surfaced 3 tensions: (1) site branded code.shebytes.io / 'Code - SheBytes.io', not carriekroutil.com; (2) deploy half-configured across S3 GH Action + Amplify + leftover GCP App Engine; (3) site code lives in ~/code/quickstart, not the carriekroutil-site repo.
- (decision) SheBytes/quickstart site is throwaway prior art (a learning rep), NOT the base to evolve. carriekroutil.com is a fresh build. What likely carries forward = the proven approach (Hugo static site, markdown-file-and-push authoring, AWS static hosting), not the branding/content.
- (decision) Working mode = Fast path. Wedge/launch post = a build-in-public piece: 'I built carriekroutil.com greenfield with the BMAD method, here's how easy it was' (teaches others; doubles as proof-of-work for recruiters).
- (decision) Confirmed: host = AWS Amplify (builds from git push) + Route 53 (domain already set up, quick win). Site code WILL move into carriekroutil-site repo (now in-scope, not deferred). Cadence number kept loose. Analytics: Mixpanel ruled out as overkill for static site; recommended GoatCounter (free) or Cloudflare Web Analytics — awaiting Carrie pick. Theme: recommended PaperMod (alts Congo, Stack) — awaiting Carrie confirm.
- (decision) Theme = Hextra (confirmed; considered/rejected PaperMod, Stack, LoveIt/FixIt, Blowfish). Scope = posts + about ONLY; docs section considered then dropped (Hextra still supports docs later if wanted). Analytics = deliberate OPEN decision, not skipped — flagged in brief as 'pick a lightweight tool soon' (GoatCounter/Cloudflare candidates).
- (event) Finalized brief (status=final). Memlog audit: all decisions captured — product framing/audience (brief), brownfield tech state + theme trail + open build questions (addendum), host/repo/theme/analytics/cadence decisions (brief). No process noise unaccounted. Editorial structure+prose pass applied directly (right-sized for 1-page brief).
