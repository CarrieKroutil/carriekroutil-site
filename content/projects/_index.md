---
title: "Projects"
# The projects section (Epic 6). Like posts/, this is the ONLY home for section-level
# config — no manual list/curation here. Public URL stays /projects/ (derived from the
# content dir). Hugo routes the section list to layouts/projects/list.html and each project
# to layouts/projects/single.html by the section name "projects" (no `type` needed).
---

A few things I've built — from a hand-coded Go site written before the AI era, to this
very site planned spec-first with an AI agent. Newest first.
